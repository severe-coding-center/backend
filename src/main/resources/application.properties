# =========================================
# APPLICATION INFO
# =========================================
spring.application.name=Back
server.port=8080

# [??] ?? ? ?? ????
spring.config.import=classpath:application-API-kEY.properties

# =========================================
# DATABASE CONFIGURATION
# =========================================
spring.datasource.url=jdbc:mysql://localhost:3306/Guard_DB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=Guard_Admin
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================================
# JPA & HIBERNATE
# =========================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =========================================
# OAUTH2 CONFIGURATION
# =========================================
# Kakao (? ??)
kakao.redirect-uri=http://localhost:8080/api/auth/login/kakao/callback

# Google (? ?? - ?? ??? ?? ???!)
google.redirect-uri=http://localhost:8080/api/auth/login/google/callback

# =========================================
# FRONTEND CONFIGURATION
# =========================================
admin.web.url=http://localhost:5173/auth/callback

# FCM Key Path
fcm.key.path=src/main/resources/fcm-key.json